const $ = (s, root=document) => root.querySelector(s);
const $$ = (s, root=document) => [...root.querySelectorAll(s)];
const app = $('#app');
const state = { center:null, fields:[], selectedField:null, selectedSlot:null, me:null, manager:null, centers:[] };
const money = c => new Intl.NumberFormat('it-IT',{style:'currency',currency:'EUR'}).format((c||0)/100);
const qs = new URLSearchParams(location.search);
const view = qs.get('view') || 'app';
const centerSlug = qs.get('center') || localStorage.getItem('last_center') || 'demo-sport';
const esc = s => String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[c]));

async function api(action, options={}){
  const res = await fetch(`/api?action=${action}`, {credentials:'include', ...options, headers:{'content-type':'application/json', ...(options.headers||{})}});
  const data = await res.json().catch(()=>({ok:false,error:'Risposta non valida'}));
  if(!res.ok || data.ok===false) throw new Error(data.error || 'Errore');
  return data;
}
function toast(message,type='ok'){
  let el=document.createElement('div'); el.className=`notice ${type}`; el.textContent=message; el.style.cssText='position:fixed;right:18px;top:18px;z-index:50;max-width:360px;box-shadow:0 10px 30px rgba(0,0,0,.12)'; document.body.append(el); setTimeout(()=>el.remove(),3200);
}
function setAccent(c){ document.documentElement.style.setProperty('--accent',c||'#111827'); document.querySelector('meta[name=theme-color]')?.setAttribute('content',c||'#111827'); }

async function loadPublic(){
  const data=await api(`center&slug=${encodeURIComponent(centerSlug)}`); state.center=data.center; state.fields=data.fields; localStorage.setItem('last_center',centerSlug); setAccent(state.center.accent_color); renderPublic();
}
function renderPublic(){
  const c=state.center;
  app.innerHTML=`<main class="shell">
    <header class="topbar"><div class="brand"><div class="logo">${c.logo_url?`<img src="${esc(c.logo_url)}">`:'S'}</div><div>${esc(c.name)}</div></div><div class="nav-actions"><button class="btn" data-scroll="conventions">Convenzioni</button><a class="btn primary" href="#book">Prenota</a></div></header>
    <section class="hero" style="background-image:url('${esc(c.cover_url||'')}')"><div class="hero-content"><span class="pill">Prenotazioni online</span><h1>${esc(c.name)}</h1><p>${esc(c.description||'Scegli il campo, l’orario e prenota in pochi secondi.')}</p><a class="btn primary" href="#book">Trova un campo</a></div></section>
    <section class="section"><div class="section-head"><div><div class="muted">I nostri spazi</div><h2>Campi disponibili</h2></div></div><div class="grid">${state.fields.map(f=>`<article class="card field-card"><span class="pill">${esc(f.sport)}</span><h3 style="margin-top:12px">${esc(f.name)}</h3><div class="muted">${esc(f.surface||'')} ${f.indoor?'• Coperto':'• Scoperto'}</div><div class="price">${money(f.price_cents)} <span class="muted" style="font-size:13px;font-weight:600">/ ${f.duration_minutes} min</span></div><button class="btn primary" style="margin-top:14px" data-book-field="${f.id}">Prenota</button></article>`).join('')}</div></section>
    <section class="section booking-box" id="book"><div><div class="muted">Prenota</div><h2 style="margin-top:4px">Scegli giorno e orario</h2><p class="muted">Il pagamento avviene direttamente con il gestore.</p></div><div class="card"><div class="form-grid"><div class="field"><label>Campo</label><select class="input" id="fieldSelect"><option value="">Seleziona</option>${state.fields.map(f=>`<option value="${f.id}">${esc(f.name)} · ${esc(f.sport)}</option>`).join('')}</select></div><div class="field"><label>Data</label><input class="input" type="date" id="bookingDate" min="${new Date().toISOString().slice(0,10)}"></div></div><div id="slots" class="slots" style="margin-top:14px"></div><div id="customerFields" class="form-grid hidden" style="margin-top:16px"><div class="field"><label>Nome</label><input class="input" id="customerName"></div><div class="field"><label>Telefono</label><input class="input" id="customerPhone" inputmode="tel"></div><div class="field full"><label>Email <span class="muted">(facoltativa)</span></label><input class="input" id="customerEmail" type="email"></div><div class="field full"><button class="btn primary" id="confirmBooking">Conferma prenotazione</button></div></div></div></section>
    ${c.conventions_enabled?`<section class="section" id="conventions"><div class="section-head"><div><div class="muted">Per squadre e gruppi</div><h2>Convenzioni sportive</h2></div></div><div class="booking-box"><div class="card"><h3>Giocate con regolarità?</h3><p class="muted">Invia una richiesta al centro indicando giorni, orari e periodo. La richiesta non blocca automaticamente il campo.</p><div class="row">${c.whatsapp?`<a class="btn" target="_blank" href="https://wa.me/${String(c.whatsapp).replace(/\D/g,'')}?text=${encodeURIComponent('Ciao, vorrei informazioni sulle convenzioni sportive.')}">Parla su WhatsApp</a>`:''}${c.phone?`<a class="btn" href="tel:${esc(c.phone)}">Chiama</a>`:''}</div></div><form class="card form-grid" id="conventionForm"><div class="field"><label>Gruppo / squadra</label><input class="input" name="group_name" required></div><div class="field"><label>Referente</label><input class="input" name="contact_name" required></div><div class="field"><label>Telefono</label><input class="input" name="phone" required></div><div class="field"><label>Campo</label><select class="input" name="field_id"><option value="">Da concordare</option>${state.fields.map(f=>`<option value="${f.id}">${esc(f.name)}</option>`).join('')}</select></div><div class="field"><label>Giorni preferiti</label><input class="input" name="preferred_days" placeholder="Es. mercoledì"></div><div class="field"><label>Orario</label><input class="input" name="preferred_times" placeholder="Es. 20:00–21:00"></div><div class="field"><label>Frequenza</label><input class="input" name="frequency" placeholder="Es. ogni settimana"></div><div class="field"><label>Periodo</label><input class="input" name="period_text" placeholder="Es. ottobre–maggio"></div><div class="field full"><label>Note</label><textarea class="input" name="notes" rows="3"></textarea></div><div class="field full"><button class="btn primary">Invia richiesta</button></div></form></div></section>`:''}
    <footer class="section card"><div class="row"><div><strong>${esc(c.name)}</strong><div class="muted">${esc(c.address||'')}</div></div><div class="spacer"></div>${c.phone?`<a class="btn" href="tel:${esc(c.phone)}">${esc(c.phone)}</a>`:''}</div></footer>
  </main>`;
  bindPublic();
}
function bindPublic(){
  $$('[data-scroll]').forEach(b=>b.onclick=()=>document.getElementById(b.dataset.scroll)?.scrollIntoView({behavior:'smooth'}));
  $$('[data-book-field]').forEach(b=>b.onclick=()=>{ $('#fieldSelect').value=b.dataset.bookField; $('#book').scrollIntoView({behavior:'smooth'}); loadSlots(); });
  $('#fieldSelect').onchange=loadSlots; $('#bookingDate').onchange=loadSlots;
  $('#confirmBooking').onclick=confirmBooking;
  $('#conventionForm')?.addEventListener('submit',async e=>{e.preventDefault(); const fd=Object.fromEntries(new FormData(e.currentTarget)); try{await api('convention',{method:'POST',body:JSON.stringify({...fd,center_id:state.center.id})}); e.currentTarget.reset(); toast('Richiesta inviata al gestore');}catch(err){toast(err.message,'error')}});
}
async function loadSlots(){
  const fieldId=$('#fieldSelect').value,date=$('#bookingDate').value; state.selectedSlot=null; $('#customerFields').classList.add('hidden'); if(!fieldId||!date){$('#slots').innerHTML='';return}
  try{const data=await api(`availability&field_id=${fieldId}&date=${date}`); state.selectedField=data.field; $('#slots').innerHTML=data.slots.map(s=>`<button class="slot" ${s.available?'':'disabled'} data-slot="${s.start}">${s.start}</button>`).join(''); $$('.slot:not(:disabled)').forEach(b=>b.onclick=()=>{ $$('.slot').forEach(x=>x.classList.remove('selected')); b.classList.add('selected'); state.selectedSlot=b.dataset.slot; $('#customerFields').classList.remove('hidden'); });}catch(e){toast(e.message,'error')}
}
async function confirmBooking(){
  if(!state.selectedSlot) return; const payload={field_id:Number($('#fieldSelect').value),date:$('#bookingDate').value,start_time:state.selectedSlot,customer_name:$('#customerName').value,customer_phone:$('#customerPhone').value,customer_email:$('#customerEmail').value};
  try{await api('booking',{method:'POST',body:JSON.stringify(payload)}); toast('Prenotazione confermata'); await loadSlots(); $('#customerName').value='';$('#customerPhone').value='';$('#customerEmail').value='';}catch(e){toast(e.message,'error')}
}

function renderLogin(){
  app.innerHTML=`<div class="login-wrap"><form class="login-card" id="loginForm"><div class="logo">S</div><h1>Accesso gestionale</h1><p class="muted">ADMIN centrale e pannello gestore usano lo stesso accesso.</p><div class="stack"><div class="field"><label>Email</label><input class="input" type="email" name="email" required></div><div class="field"><label>Password</label><input class="input" type="password" name="password" required></div><button class="btn primary">Accedi</button><a class="btn" href="/?center=${encodeURIComponent(centerSlug)}">Torna all'app</a></div></form></div>`;
  $('#loginForm').onsubmit=async e=>{e.preventDefault();const fd=Object.fromEntries(new FormData(e.currentTarget));try{const r=await api('login',{method:'POST',body:JSON.stringify(fd)});location.href=r.role==='admin'?'/?view=admin':'/?view=manager';}catch(err){toast(err.message,'error')}};
}
async function loadMe(){ try{const r=await api('me'); state.me=r.session; return r.session}catch{return null} }
function layout(role,title,content){return `<div class="app-layout"><aside class="sidebar"><div class="brand"><div class="logo">S</div><span>Sport Admin</span></div><nav class="side-nav"><button class="active">${role==='admin'?'Centri':'Dashboard'}</button><button id="logoutBtn">Esci</button></nav></aside><main class="workspace"><div class="topbar"><div><div class="muted">${role==='admin'?'Piattaforma':'Pannello gestore'}</div><h1>${esc(title)}</h1></div><button class="btn" id="logoutTop">Esci</button></div>${content}</main></div>`}
function bindLogout(){['logoutBtn','logoutTop'].forEach(id=>{const el=document.getElementById(id); if(el) el.onclick=async()=>{await api('logout',{method:'POST',body:'{}'});location.href='/?view=login'}})}

async function loadAdmin(){
  const me=await loadMe(); if(!me||me.role!=='admin') return renderLogin(); const data=await api('admin-centers'); state.centers=data.centers;
  const content=`<div class="row"><button class="btn primary" id="newCenter">+ Nuovo centro</button><button class="btn" id="newManager">+ Gestore</button><button class="btn" id="newField">+ Campo</button></div><div class="stats"><div class="card stat"><span class="muted">Centri</span><strong>${state.centers.length}</strong></div><div class="card stat"><span class="muted">Attivi</span><strong>${state.centers.filter(c=>c.active).length}</strong></div><div class="card stat"><span class="muted">Campi</span><strong>${state.centers.reduce((a,c)=>a+c.fields_count,0)}</strong></div><div class="card stat"><span class="muted">Prenotazioni</span><strong>${state.centers.reduce((a,c)=>a+c.bookings_count,0)}</strong></div></div><div class="card"><div class="section-head"><div><h2>I tuoi centri</h2><div class="muted">Un'unica piattaforma, branding separato.</div></div></div><div class="stack">${state.centers.map(c=>`<div class="center-card card"><div class="logo" style="background:${esc(c.accent_color)}">${c.logo_url?`<img src="${esc(c.logo_url)}">`:'S'}</div><div><strong>${esc(c.name)}</strong><div class="muted">/${esc(c.slug)} · ${c.fields_count} campi · ${c.bookings_count} prenotazioni</div></div><div class="spacer"></div><a class="btn small" target="_blank" href="/?center=${encodeURIComponent(c.slug)}">Apri app</a><button class="btn small" data-edit-center="${c.id}">Modifica</button></div>`).join('')}</div></div>`;
  app.innerHTML=layout('admin','Dashboard ADMIN',content);bindLogout();bindAdmin();
}
function modal(html){const d=document.createElement('div');d.className='modal';d.innerHTML=`<div class="modal-card">${html}</div>`;d.onclick=e=>{if(e.target===d)d.remove()};document.body.append(d);return d}
function centerForm(c={}){const m=modal(`<div class="section-head"><h2>${c.id?'Modifica centro':'Nuovo centro'}</h2><button class="btn small" data-close>Chiudi</button></div><form id="centerForm" class="form-grid"><input type="hidden" name="id" value="${c.id||''}"><div class="field"><label>Nome</label><input class="input" name="name" value="${esc(c.name||'')}" required></div><div class="field"><label>Slug</label><input class="input" name="slug" value="${esc(c.slug||'')}" placeholder="campizzi" required></div><div class="field"><label>Colore</label><input class="input" type="color" name="accent_color" value="${c.accent_color||'#111827'}"></div><div class="field"><label>Logo URL</label><input class="input" name="logo_url" value="${esc(c.logo_url||'')}"></div><div class="field full"><label>Copertina URL</label><input class="input" name="cover_url" value="${esc(c.cover_url||'')}"></div><div class="field"><label>Telefono</label><input class="input" name="phone" value="${esc(c.phone||'')}"></div><div class="field"><label>WhatsApp</label><input class="input" name="whatsapp" value="${esc(c.whatsapp||'')}"></div><div class="field"><label>Email</label><input class="input" name="email" value="${esc(c.email||'')}"></div><div class="field"><label>Indirizzo</label><input class="input" name="address" value="${esc(c.address||'')}"></div><div class="field full"><label>Descrizione</label><textarea class="input" name="description">${esc(c.description||'')}</textarea></div><label><input type="checkbox" name="conventions_enabled" ${c.conventions_enabled!==0?'checked':''}> Convenzioni</label><label><input type="checkbox" name="recurring_enabled" ${c.recurring_enabled!==0?'checked':''}> Ricorrenti</label><label><input type="checkbox" name="active" ${c.active!==0?'checked':''}> Centro attivo</label><div class="field full"><button class="btn primary">Salva centro</button></div></form>`); $('[data-close]',m).onclick=()=>m.remove(); $('#centerForm',m).onsubmit=async e=>{e.preventDefault();const fd=Object.fromEntries(new FormData(e.currentTarget));fd.id=Number(fd.id||0);fd.conventions_enabled=e.currentTarget.conventions_enabled.checked;fd.recurring_enabled=e.currentTarget.recurring_enabled.checked;fd.active=e.currentTarget.active.checked;try{await api('admin-center-save',{method:'POST',body:JSON.stringify(fd)});m.remove();toast('Centro salvato');loadAdmin()}catch(err){toast(err.message,'error')}}}
function bindAdmin(){ $('#newCenter').onclick=()=>centerForm(); $$('[data-edit-center]').forEach(b=>b.onclick=()=>centerForm(state.centers.find(c=>c.id==b.dataset.editCenter))); $('#newManager').onclick=()=>managerForm(); $('#newField').onclick=()=>fieldForm(); }
function managerForm(){const m=modal(`<div class="section-head"><h2>Nuovo gestore</h2><button class="btn small" data-close>Chiudi</button></div><form id="managerForm" class="form-grid"><div class="field full"><label>Centro</label><select class="input" name="center_id">${state.centers.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>Nome</label><input class="input" name="name" required></div><div class="field"><label>Email</label><input class="input" name="email" type="email" required></div><div class="field full"><label>Password iniziale</label><input class="input" name="password" required></div><div class="field full"><button class="btn primary">Crea accesso</button></div></form>`);$('[data-close]',m).onclick=()=>m.remove();$('#managerForm',m).onsubmit=async e=>{e.preventDefault();try{await api('admin-manager-create',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget))) });m.remove();toast('Gestore creato')}catch(err){toast(err.message,'error')}}}
function fieldForm(){const m=modal(`<div class="section-head"><h2>Nuovo campo</h2><button class="btn small" data-close>Chiudi</button></div><form id="fieldForm" class="form-grid"><div class="field full"><label>Centro</label><select class="input" name="center_id">${state.centers.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>Nome campo</label><input class="input" name="name" required></div><div class="field"><label>Disciplina</label><input class="input" name="sport" placeholder="Padel, calcio a 7..." required></div><div class="field"><label>Superficie</label><input class="input" name="surface"></div><div class="field"><label>Prezzo €</label><input class="input" type="number" step=".01" name="price" required></div><div class="field"><label>Durata (min)</label><input class="input" type="number" name="duration_minutes" value="60"></div><div class="field"><label>Apertura</label><input class="input" type="time" name="opening_time" value="08:00"></div><div class="field"><label>Chiusura</label><input class="input" type="time" name="closing_time" value="23:00"></div><label><input type="checkbox" name="indoor"> Coperto</label><div class="field full"><button class="btn primary">Aggiungi campo</button></div></form>`);$('[data-close]',m).onclick=()=>m.remove();$('#fieldForm',m).onsubmit=async e=>{e.preventDefault();const fd=Object.fromEntries(new FormData(e.currentTarget));fd.indoor=e.currentTarget.indoor.checked;try{await api('admin-field-save',{method:'POST',body:JSON.stringify(fd)});m.remove();toast('Campo creato');loadAdmin()}catch(err){toast(err.message,'error')}}}

async function loadManager(){
  const me=await loadMe(); if(!me||me.role!=='manager') return renderLogin(); const d=await api('manager-dashboard'); state.manager=d; setAccent(d.center.accent_color);
  const today=new Date().toISOString().slice(0,10), upcoming=d.bookings.filter(b=>b.date>=today&&b.status!=='cancelled');
  const content=`<div class="row"><button class="btn primary" id="manualBooking">+ Prenotazione</button>${d.center.recurring_enabled?'<button class="btn" id="recurringBooking">+ Ricorrente</button>':''}</div><div class="stats"><div class="card stat"><span class="muted">Prossime</span><strong>${upcoming.length}</strong></div><div class="card stat"><span class="muted">Oggi</span><strong>${upcoming.filter(b=>b.date===today).length}</strong></div><div class="card stat"><span class="muted">Da incassare</span><strong>${upcoming.filter(b=>b.payment_status==='due'&&b.status==='confirmed').length}</strong></div><div class="card stat"><span class="muted">Convenzioni</span><strong>${d.conventions.filter(x=>x.status==='new').length}</strong></div></div><section class="card"><div class="section-head"><div><h2>Calendario prenotazioni</h2><div class="muted">Prenotazioni app, telefoniche e ricorrenti.</div></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Data</th><th>Ora</th><th>Campo</th><th>Cliente</th><th>Pagamento</th><th>Stato</th><th></th></tr></thead><tbody>${d.bookings.map(b=>`<tr><td>${b.date}</td><td>${b.start_time}–${b.end_time}</td><td>${esc(b.field_name)}</td><td>${esc(b.customer_name)}<div class="muted">${esc(b.customer_phone)}</div></td><td><span class="status ${b.payment_status}">${b.payment_status==='paid'?'Pagato':'Da pagare'}</span></td><td><span class="status ${b.status}">${b.status}</span></td><td><div class="row"><button class="btn small" data-paid="${b.id}">${b.payment_status==='paid'?'Da pagare':'Segna pagato'}</button>${b.status!=='cancelled'?`<button class="btn small danger" data-cancel="${b.id}">Annulla</button>`:''}</div></td></tr>`).join('')}</tbody></table></div></section>${d.center.conventions_enabled?`<section class="card section"><div class="section-head"><div><h2>Richieste convenzione</h2><div class="muted">Gestisci le richieste prima di creare la ricorrenza.</div></div></div><div class="stack">${d.conventions.length?d.conventions.map(x=>`<div class="card"><div class="row"><div><strong>${esc(x.group_name)}</strong><div class="muted">${esc(x.contact_name)} · ${esc(x.phone)} · ${esc(x.field_name||'Campo da concordare')}</div><div style="margin-top:8px">${esc(x.preferred_days)} ${esc(x.preferred_times)} · ${esc(x.frequency)} · ${esc(x.period_text)}</div></div><div class="spacer"></div><select class="input" style="width:auto" data-conv="${x.id}"><option value="new" ${x.status==='new'?'selected':''}>Nuova</option><option value="negotiating" ${x.status==='negotiating'?'selected':''}>In trattativa</option><option value="accepted" ${x.status==='accepted'?'selected':''}>Accettata</option><option value="rejected" ${x.status==='rejected'?'selected':''}>Rifiutata</option></select></div></div>`).join(''):'<div class="empty">Nessuna richiesta.</div>'}</div></section>`:''}`;
  app.innerHTML=layout('manager',d.center.name,content);bindLogout();bindManager();
}
function bindManager(){ $('#manualBooking').onclick=()=>bookingModal(false); $('#recurringBooking')?.addEventListener('click',()=>bookingModal(true)); $$('[data-paid]').forEach(b=>b.onclick=async()=>{const row=state.manager.bookings.find(x=>x.id==b.dataset.paid);await api('manager-booking-status',{method:'POST',body:JSON.stringify({id:row.id,payment_status:row.payment_status==='paid'?'due':'paid'})});loadManager()}); $$('[data-cancel]').forEach(b=>b.onclick=async()=>{if(!confirm('Annullare questa prenotazione?'))return;await api('manager-booking-status',{method:'POST',body:JSON.stringify({id:Number(b.dataset.cancel),status:'cancelled'})});loadManager()}); $$('[data-conv]').forEach(s=>s.onchange=async()=>{await api('manager-convention-status',{method:'POST',body:JSON.stringify({id:Number(s.dataset.conv),status:s.value})});toast('Stato aggiornato')}); }
function bookingModal(recurring){const d=state.manager;const m=modal(`<div class="section-head"><h2>${recurring?'Prenotazione ricorrente':'Nuova prenotazione'}</h2><button class="btn small" data-close>Chiudi</button></div><form id="bookingForm" class="form-grid"><div class="field full"><label>Campo</label><select class="input" name="field_id">${d.fields.filter(f=>f.active).map(f=>`<option value="${f.id}">${esc(f.name)} · ${esc(f.sport)}</option>`).join('')}</select></div><div class="field"><label>Nome cliente / gruppo</label><input class="input" name="customer_name" required></div><div class="field"><label>Telefono</label><input class="input" name="customer_phone"></div>${recurring?`<div class="field"><label>Dal</label><input class="input" type="date" name="start_date" required></div><div class="field"><label>Al</label><input class="input" type="date" name="end_date" required></div><div class="field full"><label>Giorno</label><select class="input" name="weekday"><option value="1">Lunedì</option><option value="2">Martedì</option><option value="3">Mercoledì</option><option value="4">Giovedì</option><option value="5">Venerdì</option><option value="6">Sabato</option><option value="0">Domenica</option></select></div>`:`<div class="field full"><label>Data</label><input class="input" type="date" name="date" required></div>`}<div class="field"><label>Dalle</label><input class="input" type="time" name="start_time" required></div><div class="field"><label>Alle <span class="muted">(facoltativo)</span></label><input class="input" type="time" name="end_time"></div><div class="field"><label>Prezzo €</label><input class="input" type="number" step=".01" name="price"></div><div class="field"><label>Pagamento</label><select class="input" name="payment_status"><option value="due">Da pagare</option><option value="paid">Pagato</option></select></div>${!recurring?'<label><input type="checkbox" name="blocked"> Blocca orario (chiusura/manutenzione)</label>':''}<div class="field full"><label>Note</label><textarea class="input" name="notes"></textarea></div><div class="field full"><button class="btn primary">${recurring?'Controlla e crea ricorrenza':'Salva prenotazione'}</button></div></form>`);$('[data-close]',m).onclick=()=>m.remove();$('#bookingForm',m).onsubmit=async e=>{e.preventDefault();const fd=Object.fromEntries(new FormData(e.currentTarget));if(!recurring)fd.blocked=e.currentTarget.blocked.checked;try{const r=await api(recurring?'manager-recurring':'manager-booking',{method:'POST',body:JSON.stringify(fd)});m.remove();if(recurring&&r.conflicts?.length)toast(`${r.created.length} date create, ${r.conflicts.length} conflitti saltati`);else toast('Prenotazione salvata');loadManager()}catch(err){toast(err.message,'error')}}}

(async()=>{try{if(view==='login')renderLogin();else if(view==='admin')await loadAdmin();else if(view==='manager')await loadManager();else await loadPublic();}catch(e){app.innerHTML=`<div class="login-wrap"><div class="login-card"><h1>Errore</h1><p class="muted">${esc(e.message)}</p><a class="btn" href="/">Riprova</a></div></div>`}})();
